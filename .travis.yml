
branches:
  only:
    - master

sudo: false

addons:
  apt:
    packages:
      - liblzma-dev

matrix:
  include:
    - os: linux
      language: python
      python: 2.7
    - os: linux
      language: python
      python: 3.5
    - os: linux
      language: python
      python: 3.6
    - os: linux
      language: python
      python: 3.7
      dist: xenial
      sudo: true
    - os: osx
      language: generic

install:
  - pip install -U tox-travis coveralls pip setuptools wheel
  - python setup.py build_ext --inplace

script:
  - tox

after_success:
  - coveralls
